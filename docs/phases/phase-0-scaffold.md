# Phase 0 — Scaffold

**Status:** ✅ Complete

## What was built

- Expo SDK 56 project scaffolded with TypeScript strict mode
- `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`, `noImplicitOverride` all enabled
- Expo Router v4 file-based routing configured
- ESLint v9 flat config (`eslint.config.js`)
- Jest 29 + jest-expo configured
- Supabase project created (ref: `ilykfwtiebfyaljlhsrx`)
- CI via GitHub Actions (`.github/workflows/ci.yml`)
